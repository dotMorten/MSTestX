<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TestAppRunner.Views"
             Title="{Binding TestStatus}"
             x:Class="TestAppRunner.Views.GroupByClassTestsPage">
    <ContentPage.Content>       
        <StackLayout>
            <ListView x:Name="list" ItemSelected="list_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="20,0">
                                <Label Text="{Binding Group, Mode=OneTime}" FontAttributes="Bold" />
                                <Label Text="{Binding TestStatus, Mode=OneWay}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Clicked="Button_Clicked" Text="Run tests" x:Name="startStopButton" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>